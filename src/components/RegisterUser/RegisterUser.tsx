import React, { FC } from 'react';


interface RegisterUserProps {}

const RegisterUser: FC<RegisterUserProps> = () => (
  <div data-testid="RegisterUser">
    RegisterUser Component
  </div>
);

export default RegisterUser;
